language: objective-c

env:
 global:
  - EnableNuGetPackageRestore=true 
 matrix:
  - MONO_VER="2.10.8_3"
  - MONO_VER="3.0.12"
  - MONO_VER="3.1.2"
  - MONO_VER="3.2.7"
  - MONO_VER="3.4.0"

before_install:
 - .ci/install-mono.sh
 - .ci/install-certs.sh

script:
 - xbuild /p:Configuration=Debug /verbosity:quiet /nologo
 - .ci/nunit.sh bin/Debug/AnyCPU/dnplatform_tests.dll
